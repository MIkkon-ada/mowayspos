# 负责人补充立项参考稿样式实施计划

## 任务 1：建立页面视觉契约

- 在 `OwnerSubmitModal.layout.test.tsx` 增加工作台、顶部、概览、导航、详情和底部操作栏的语义标记断言。
- 验证测试先失败，确保新契约确实覆盖待改造的页面结构。

## 任务 2：补齐语义标记并统一外层布局

- 给负责人补充立项工作台和主要区域添加专用 class/test id。
- 不调整已有事件处理、表单字段、接口调用和生命周期权限判断。

## 任务 3：按参考稿重做视觉层

- 在 `frontend/src/styles.css` 增加限定在 `owner-submit-reference-layout` 下的样式。
- 统一页面背景、边框、圆角、阴影、字体层级、状态色和操作色。
- 调整三栏尺寸、详情区留白、任务表头/行态、AI 面板和底部固定操作栏。
- 增加窄屏断点，确保布局不产生横向溢出。

## 任务 4：回归验证

- 运行负责人补充立项布局与 AI 面板相关测试。
- 运行完整前端测试、生产构建和 `git diff --check`。
- 通过热更新检查本地页面仍能启动；不发布、不提交远程环境。
