# 关键任务列表列宽设计

## 目标

让工作推进表更适合横向扫描：缩小“关键任务”列，为负责人、计划时间、协同人和状态保留稳定空间。

## 已确认的交互与布局

- “关键任务”列宽从 430 CSS px 调整为 360 CSS px。
- 标题仍以 13px 字体自动换行，不截断、不增加数据库长度限制。
- 完整标题继续在点击后进入的关键任务执行工作台中展示。
- 本次不调整其他列宽、数据字段、路由或权限。

## 验收

- 表格 `colgroup` 中“关键任务”第二列为 360px。
- 源码测试能防止该列重新扩展为 430px。
- 前端专项测试与生产构建通过。
