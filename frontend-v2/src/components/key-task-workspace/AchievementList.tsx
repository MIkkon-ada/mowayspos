import type { KeyTaskWorkspace } from '../../api/keyTaskWorkspace'
import { formatDateTime, statusTone } from './workspaceFormat'

export function AchievementList({ items }: { items: KeyTaskWorkspace['achievements'] }) { return <section className="min-w-0 rounded-lg border border-slate-300 border-l-4 border-l-emerald-600 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold text-slate-900">▣　成果与交付</h2>{items.length === 0 ? <div className="grid min-h-28 place-items-center text-sm text-slate-500">暂无关联成果。</div> : <div className="mt-4 overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b text-slate-500"><tr><th className="py-2 font-medium">名称</th><th className="py-2 font-medium">类型</th><th className="py-2 font-medium">状态</th><th className="py-2 font-medium">Owner</th><th className="py-2 font-medium">形成时间</th><th className="py-2 font-medium">版本</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b border-slate-100"><td className="py-3 font-medium">{item.name}</td><td className="py-3">{item.achievement_type || '—'}</td><td className="py-3"><span className={`rounded px-2 py-1 text-xs ${statusTone(item.status)}`}>{item.status || '—'}</span></td><td className="py-3">{item.owner || '—'}</td><td className="py-3">{formatDateTime(item.created_at)}</td><td className="py-3">{item.version || '—'}</td></tr>)}</tbody></table></div>}</section> }
